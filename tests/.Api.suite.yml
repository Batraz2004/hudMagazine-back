class_name: ApiTester
modules:
    enabled:
        - REST:
            url: http://localhost:81/api/v1
            depends: Laravel
        - \Helper\Api
    config:
        - Laravel:
            environment_file: .env.testing

